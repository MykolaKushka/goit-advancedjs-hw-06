let age: number = 50; // A number variable
let personName: string = 'Max'; // A string variable (renamed to avoid conflict)
let toggle: boolean = true; // A boolean variable
let empty: null = null; // A variable with a null value
let notInitialize: undefined; // A variable that is not initialized (undefined)
let callback: (a: number) => number = (a) => { return 100 + a }; // A function that takes a number as input and returns a number
